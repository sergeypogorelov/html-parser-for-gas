{
  "name": "html-parser-for-gas",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "scripts": {
    "build": "rimraf dist && webpack --config webpack.config.js",
    "push": "npm run build && clasp push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sergey.pogorelov.93@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "copy-webpack-plugin": "^11.0.0",
    "gas-webpack-plugin": "^2.5.0",
    "rimraf": "^5.0.1",
    "webpack": "^5.85.0",
    "webpack-cli": "^5.1.1"
  }
}
